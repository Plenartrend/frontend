name: "Deploy Docker Images to Nexus"

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  push_to_registry:
    name: Push Docker image to Nexus
    runs-on: self-hosted
    environment: production
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Log in to Nexus Docker registry
        uses: docker/login-action@v3
        with:
          registry: registry.sokutan.de
          username: ${{ secrets.NEXUS_USERNAME }}
          password: ${{ secrets.NEXUS_PASSWORD }}

      - name: Build and push Docker image frontend
        uses: docker/build-push-action@v6
        with:
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: registry.sokutan.de/plenartrend/frontend:${{ github.sha }}

      - name: Setup SSH key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.ARGO_SSH_PRIVATE_KEY }}

      - name: Clone manifests repository
        run: |
          git clone git@github.com:Plenartrend/infrastructure.git manifests-repo
          cd manifests-repo
          git config user.name "GitHub Actions Frontend"
          git config user.email "actions@github.com"

      - name: Update deployment manifest
        run: |
          cd manifests-repo
          IMAGE_TAG="${{ github.sha }}"
          sed -i "s|image: registry.sokutan.de/plenartrend/frontend:.*|image: registry.sokutan.de/plenartrend/frontend:${IMAGE_TAG}|" manifests/frontend/frontend-deployment.yml

      - name: Commit and push changes
        run: |
          cd manifests-repo
          git add manifests/frontend/frontend-deployment.yml
          git commit -m "Update frontend deployment to ${{ github.sha }}" || exit 0
          git push origin main
